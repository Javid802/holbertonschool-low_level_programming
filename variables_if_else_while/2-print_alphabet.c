#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Prints the last digit of a random number and its properties
 *
 * Return: Always 0 (Success)
 */
int main(void)
{   
    char letter1 = 'a';
    while (letter1 <= 'z')
    {
    putchar(letter1);
    letter1++;
    }
    printf("\n");
    return 0;

}
