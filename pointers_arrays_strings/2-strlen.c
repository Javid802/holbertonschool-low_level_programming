#include "main.h"

/**
 * swap_int - swaps the values of two integers
 * @a: pointer to first int
 * @b: pointer to second int
 */
int _strlen(char *s)
{
    int a = 0;
    while (s[a]!='\0')
    {
        a++;
    }
    return(a);
}
