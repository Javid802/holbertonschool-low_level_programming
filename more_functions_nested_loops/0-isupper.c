#include "main.h"
#include <stdio.h>

/**
 * print_to_98 - prints all natural numbers from n to 98
 * @n: starting number
 */
int _isupper(int c)
{
    if (c > "A" && c<"Z")
    {
        return 1;
    }
    return 0;
}
